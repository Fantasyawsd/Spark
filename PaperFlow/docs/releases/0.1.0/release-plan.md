# PaperFlow 0.1.0 单机版发布计划

> 状态：功能代码候选；等待正式签名、Android 真机与商店发布门
> 最近更新：2026-08-01
> 第一验收平台：Android 手机

## 1. 0.1.0 产品范围

0.1.0 只保留三个一级界面：

1. **论文**：远程论文流、分类、搜索、阅读、翻译、点赞、收藏、评论和分享。
2. **ChatPaper**：置顶主聊天、按论文建立的聊天、流式回答、深度思考、联网搜索和本地会话管理。
3. **我的**：收藏、阅读历史、稍后阅读、主题、AI 凭据、本地数据和应用信息。

以下能力不进入 0.1.0：

- 社区信息流、发帖和社区互动；
- 用户私信、系统通知和未读提醒；
- 账号、云同步和跨设备状态；
- 论文上传、内容发布和审核。

现有 `community` 和旧消息模块暂时保留源码，但不进入正式导航和生产组合根。确认 0.1.0 稳定后再决定归档或删除，避免在收敛阶段进行无关的破坏性清理。

## 2. 架构整改顺序

远程功能接入前先完成以下结构调整：

1. 领域层使用 `Paper`，不再以数据库语义的 `PaperRecord` 作为业务实体。
2. 作者使用结构化列表，计数使用整数；紧凑数字只在展示层格式化。
3. 论文正文和统计值使用独立领域值对象，远程 JSON 由数据层 DTO/Mapper 转换。
4. `PaperRepository` 升级为异步、分页接口；Widget 只依赖 Controller 状态。
5. 远程、缓存和种子数据分别作为 Repository Adapter，由组合根选择和组装。
6. AI 凭据通过独立端口读取，DeepSeek Adapter 不直接读取页面状态或本地文件。

每次整改必须保持现有功能，执行 `dart analyze`、相关测试、全量 `flutter test` 和 `git diff --check`，并使用原子提交。

## 3. 远程论文数据源

0.1.0 采用客户端可直接访问的公开论文源，首选 arXiv 搜索/详情接口。OpenAlex 只作为可选的引用数和概念增强源，不阻塞论文阅读。

Repository 对外提供：

- 推荐、最新和领域筛选；
- 关键词搜索；
- 游标或页码分页；
- 下拉刷新和下一页预加载；
- 论文详情查询；
- 缓存优先、远程更新；
- 无网时回退到最近缓存或内置种子；
- 明确的加载、空结果、网络错误和重试状态。

数据链路：

```text
远程 JSON/XML -> Provider DTO -> Mapper -> Paper
                                  ↓
                         版本化本地缓存 Record
```

远程 DTO、缓存 Record 和领域 `Paper` 不共用同一个类型。

## 4. DeepSeek 配置与密钥

DeepSeek 的接口地址、模型标识和协议版本可以作为应用常量固定；**可公开分发的 APK 不硬编码共享 API Key**。APK 中的字符串和编译常量可被提取，硬编码 Key 会导致额度盗用，无法满足“可上线”要求。

0.1.0 单机版采用 BYOK：

- 用户在“我的 > AI 设置”中输入 DeepSeek API Key；
- Key 使用 Android Keystore 支持的安全存储，不写普通 JSON、不提交 Git；
- 页面只显示掩码和连接状态；
- 支持验证、替换和删除 Key；
- 未配置或鉴权失败时提供明确引导；
- Endpoint、模型和默认请求参数由代码固定；
- 私人开发构建可以继续使用 `--dart-define` 作为调试回退，但不作为商店发布方案。

如果未来改为所有用户共享平台额度，必须增加服务端代理、鉴权、限流和额度控制。

## 5. 三个界面的上线闭环

### 5.1 论文

- [x] 远程推荐论文流：生产组合根接入 arXiv Atom API，启动时先显示种子数据，再异步更新远程或缓存页
- [x] 推荐领域筛选下推到 arXiv 查询；宽领域映射为一个或多个 arXiv 分类，切换中的旧请求不会覆盖当前筛选
- [x] 远程搜索、分页、错误回退：搜索页可通过 `PaperCatalogRepository` 查询远程结果，并支持加载更多
- [x] 最近数据缓存与离线回退：远程成功写入版本化论文缓存；失败时按查询缓存、内置种子顺序回退
- [x] Markdown、公式、中文翻译和相关论文
- [x] 首页 Feed 与外部全屏阅读视角分离；搜索、收藏、历史和相关论文压栈打开，返回原入口且不改变 Feed 位置
- [x] 点赞、收藏、评论、分享和阅读状态本地持久化
- [x] 收藏支持默认分组、自定义分组、重命名和删除；单击切换默认收藏，长按选择分组
- [x] 远程数据变化后仍以稳定论文 ID 关联本地状态：arXiv ID 去除版本后缀，缓存 Record 与领域 `Paper` 分层转换
- [ ] Android 真机弱网、断网、真实 arXiv 分页和刷新体验验收
- [ ] Android 系统分享面板真机验收

### 5.2 ChatPaper

- [x] 主聊天与论文聊天共用聊天领域模块
- [x] SSE 流式正文、思考过程、联网来源、停止和重试
- [x] Markdown、公式和可复制代码块
- [x] 会话本地保存、置顶、删除和清空上下文
- [x] 使用安全凭据仓储动态创建 DeepSeek Adapter；聊天、联网搜索和翻译在每次请求前读取当前 Key
- [x] Key 缺失、鉴权失败、限流和网络失败的明确状态
- [x] 主聊天和论文聊天的会话入口统一为 0.1.0 一级“ChatPaper”页面

### 5.3 我的

- [x] 收藏、阅读历史和稍后阅读
- [x] 按收藏分组浏览论文并管理自定义分组
- [x] 主题设置
- [x] 主题设置使用版本化本地存储，重启恢复并随业务数据重置
- [x] DeepSeek Key 配置、验证、替换和删除；Android 使用 Keystore 支持的安全存储
- [ ] Android 真机验证 Key 在重启、升级、卸载和备份关闭状态下的行为
- [x] 论文缓存、中文解读、聊天和阅读互动数据占用展示
- [x] 分类清理论文缓存、ChatPaper 对话以及重置本地业务数据；DeepSeek Key 独立保留
- [x] 删除身份、VIP、社交、通知、帖子、草稿和下载 Demo
- [x] 本地数据摘要、隐私说明、版本和开源许可
- [x] “我的”展示当前论文来源、在线/离线状态和最近抓取时间

## 6. 发布验收

Android 0.1.0 身份已固定为 `app.paperflow.reader`，正式 release 不再回退到 debug 签名。签名信息仅从被 Git 忽略的 `android/key.properties` 读取，仓库保留 `android/key.properties.example` 作为本地配置模板。

缺少签名配置时 release Gradle 任务会明确失败；Android 已禁用自动备份和明文流量。开发期 DeepSeek 辅助脚本拒绝 release，产品构建也不会读取 `DEEPSEEK_API_KEY` 编译常量，避免把开发 Key 打入安装包。

- 冷启动有本地内容；联网后自动更新，不因远程失败进入空白页。
- 三个一级页面高度和 Dock 状态稳定，不出现未实现入口。
- 所有本地状态在重启后恢复，损坏数据可隔离并回退。
- 未配置 Key 时论文浏览完全可用，AI 页面提供配置引导。
- AI 请求支持流式、停止、重试；密钥不会出现在日志、普通文件或 Git diff 中。
- 无社区、私信、通知、发布按钮或相关占位文案。
- `dart analyze`、全量 `flutter test` 和静态发布检查通过。
- Android 真机完成网络、键盘、分享、后台恢复和低网速人工验收。

逐项操作和证据要求见 [`release-checklist.md`](release-checklist.md)。正式隐私政策仍需补充运营者、联系邮箱并发布为 HTTPS URL；当前 [`privacy-policy-draft.md`](privacy-policy-draft.md) 仅为草案。
