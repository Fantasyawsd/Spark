# Spark 开发路线图

> 状态：持续维护
> 最近更新：2026-08-07

本文是开发计划的唯一文件，记录产品边界、优先级、当前能力与后续方向。
发布范围和证据归入 `releases/<version>/`；架构、Git 与发布规则分别见
[`standards/code-structure.md`](standards/code-structure.md)、
[`standards/version-control.md`](standards/version-control.md)、
[`standards/release-management.md`](standards/release-management.md)。

---

## 1. 产品概览

### 1.1 产品边界

Spark 面向个人研究者，核心闭环由三个一级能力组成：

| 模块 | 职责 |
| --- | --- |
| **论文** | 发现、筛选、阅读、收藏和管理论文 |
| **ChatPaper** | 围绕通用问题或指定论文进行可恢复的 AI 对话 |
| **我的** | 管理个人研究数据、收藏分组、阅读记录、主题和 DeepSeek 凭据 |

社区与私信作为未来扩展功能保留（见第 5 节），当前不进入生产导航。
通知、账号体系、云同步和内容发布暂不进入当前生产导航——只有在重新确认产品价值、数据来源和维护成本后才纳入规划。

### 1.2 持续优先级

| 优先级 | 方向 | 说明 |
| --- | --- | --- |
| **P0** | 单机可用与发布质量 | 三入口完整单机闭环；本地数据可恢复、可清理、可迁移；密钥不进入源码；构建与测试可重复 |
| **P1** | 论文发现与结构化阅读 | 可扩展频道模型；结构化元数据；六页阅读信息架构；PDF AI 解读 |
| **P2** | ChatPaper 研究工作流 | 统一数据模型与上下文边界；稳定流式渲染；为服务端代理保留接口 |
| **P3** | 个人研究资料管理 | 完善收藏、历史、导出与备份；为账号同步建立领域契约 |

---

## 2. 当前状态

### 2.1 论文

- ✅ 论文 / ChatPaper / 我的三个一级页面，本地功能形成完整单机闭环
- ✅ 正式组合根接入 arXiv Atom 远程目录、版本化论文缓存与离线回退，内置种子保证冷启动
- ✅ 结构化频道体系：推荐 / 关注 / 最新固定频道 + 完整 arXiv `cs.*` 用户主题目录
- ✅ 频道栏与「主题 / 会议」Tab 管理页已上线；频道偏好版本化持久化
- ✅ 每频道独立论文列表、浏览位置、发布时间筛选与懒加载
- ✅ 论文元数据语义已拆分（source / venue / journalReference / comment / affiliations / subjects）
- ✅ 未知单位与未知引用数不再显示占位数据；生产 arXiv 链路不产生引用数，展示入口保留
- ✅ 本地搜索、点赞、收藏、评论、分享、阅读历史、稍后阅读和自定义收藏分组已持久化
- ✅ 搜索框支持 arXiv ID / abs / PDF 链接直达论文
- ✅ DeepSeek BYOK 流式翻译与对话；密钥存设备安全存储
- ✅ Abstract / 摘要 / 关键词 / 作者 / AI 解读 / 相关论文六页阅读器，标签横向滚动且切换论文回到 Abstract
- ✅ 中文摘要和内容关键词按需生成并独立版本化缓存；有效关键词会进入 ChatPaper 上下文
- ✅ 行内 LaTeX 支持：公式、前后正文和标点保持同一段排版；Abstract 文本可选
- ✅ 主要 UI 流程有 Widget 测试覆盖，Android debug APK 可重复构建

**当前边界：**
- 无账号、无跨设备同步、无服务端代理；所有互动状态均为本地
- 相关论文使用结构化本地关系，未接入真实引用图谱
- AI 上下文默认只含论文元信息、摘要和有效关键词；论文聊天开启“读取全文”后，可注入按预算裁剪并带页码引用的 PDF 文本，仍不包含图表或参考文献

### 2.2 ChatPaper

- ✅ 置顶主聊天和按论文创建的聊天
- ✅ 论文 AI 会话与聊天首页共用同一会话仓储；支持置顶与滑动删除
- ✅ DeepSeek BYOK、流式回答、深度思考、联网搜索、停止和重试
- ✅ Markdown、公式、代码块、来源展示和本地会话恢复
- ✅ 增量节流渲染、公式纯文本降级、代码块复制、来源面板、未闭合语法稳定
- ✅ 移动端聊天 UI：顶部栏（可编辑标题/大纲预览）、消息气泡、推理卡片、消息操作与自适应 Composer
- ✅ 消息操作：复制（不含思考过程）、最新重试、删除多选；来源可点击跳转
- ✅ 会话级设置：自定义系统提示词、回答风格与可组合技能，独立持久化
- ✅ 论文聊天可读取全文：PDF 文本提取、分块缓存、预算裁剪与页码追溯引用
- ✅ 来源面板默认折叠；发送即收起键盘；模型头像透明化并用于二级菜单

**当前边界：**
- API Key 由用户配置并保存在设备安全存储，公开构建不含共享 Key
- 会话和论文上下文保存在本地，不支持跨设备同步或团队共享
- 论文聊天默认使用论文元信息、摘要与可选关键词；PDF 全文上下文仅在用户开启「读取全文」后注入

### 2.3 我的

- ✅ 默认收藏与自定义收藏分组（重命名与带二次确认的删除已实现，默认分组受保护）
- ✅ 收藏、稍后阅读和阅读历史可从统计卡片与“查看全部”进入完整论文列表
- ✅ 阅读历史、稍后阅读、主题设置和本地数据管理
- ✅ DeepSeek API Key 验证、保存、替换和删除
- ✅ 应用版本、隐私说明和开源许可入口

**当前边界：**
- 用户资料、收藏、历史和设置只保存在当前设备
- 不提供登录、云同步、私信、通知或公开主页
- 清理业务数据不得隐式删除 DeepSeek Key；敏感操作必须二次确认

### 2.4 社区与私信（扩展保留）

- `lib/src/features/community/`：保留 `CommunityPost` 实体、演示帖子数据与 `CommunityScreen`，仅 domain / data / presentation 三层，无 application 层、仓储和持久化
- `lib/src/features/messages/`：保留 `MessageItem` 实体与演示消息数据，presentation 层为空
- 两个模块均未在组合根、路由和底部导航中挂载，无外部引用
- `experimentalCommunity` 功能开关（环境变量 `SPARK_FEATURE_COMMUNITY`）已定义，生产环境强制为 false
- Shell 测试显式断言「社区」「私信」「通知」文案不出现在生产导航

---

## 3. 近期路线

### 3.1 论文频道与阅读

以下为当前 P1 核心待办，按实施顺序排列：

| # | 任务 | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 顶部频道栏 + 频道管理页 | ✅ 已完成 | 管理页「主题/会议」Tab，对勾即添加；搜索与排序待目录扩大后恢复 |
| 2 | arXiv 主题与会议频道数据模型 | ✅ 已完成 | 主题模型与首批目录落地；会议频道模型保留，待真实数据源 |
| 3 | 将旧筛选按钮改为时间筛选 | ✅ 已完成 | 范围按频道持久化，并进入 arXiv 查询、离线回退和缓存键 |
| 4 | source / venue / subject / publish time / 单位字段修正 | ✅ 已完成 | 缓存 schema v2 迁移同步还原未知语义 |
| 5 | 移除引用数展示 | ✅ 已完成（核心） | 未知引用数不显示、生产链路不产生引用数；展示入口保留待可靠来源 |
| 6 | 论文内容改为六个横向页面 | ✅ 已完成 | Abstract / 摘要 / 关键词 / 作者 / AI 解读 / 相关论文；见 §4.2 |
| 7 | 中文摘要缓存与摘要页文案 | ✅ 已完成 | 按需生成、版本化缓存、失败重试，界面只显示内容状态 |
| 8 | 内容关键词生成与缓存 | ✅ 已完成 | 用户触发生成 5–12 个关键词，独立缓存并校验输入指纹与 prompt 版本 |
| 9 | PDF 下载、正文提取与六问 AI 解读 | 🚧 部分完成 | PDF 下载、正文提取、分块、页码追溯与本地缓存已由论文 ChatPaper 落地；六问结构化解读与结果缓存待开始 |
| 10 | AI 解读接入论文 ChatPaper 默认上下文 | 🚧 部分完成 | 论文 AI 会话已并入聊天首页；六问结果作为默认上下文依赖第 9 条 |
| 11 | 相关论文语义检索 | ⬜ 待开始 | 暂保留页面与空状态；第一阶段用标题 + 关键词语义检索 |

### 3.2 ChatPaper

| # | 任务 | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 稳定流式 Markdown / 公式 / 代码 / 来源的增量渲染 | ✅ 已完成 | 增量节流渲染、未闭合语法稳定（含斜体/删除线）；保留持续改进空间 |
| 2 | 明确主聊天、论文聊天与 AI 解读缓存之间的数据边界 | ✅ 已完成 | 边界规则见 §4.2「ChatPaper 上下文边界」；测试覆盖主聊天/论文聊天身份与注入规则 |
| 3 | 接入 PDF 分块、可追溯引用和上下文裁剪 | ✅ 已完成（已知问题见下） | 论文聊天可读取全文：PDF 下载/文本提取/分块缓存，按预算裁剪并带页码追溯引用；依赖 syncfusion_flutter_pdf |
| 4 | 会话级系统提示词、个性化偏好与可组合 Skills | ✅ 已完成 | 会话设置面板：自定义系统提示词、回答风格（简洁/均衡/详细）与可组合技能；独立持久化 |
| 5 | 选择模型二级菜单头像化 | ✅ 已完成 | 二级菜单与 Composer 入口统一展示透明背景模型头像；加载失败保留内置图标 fallback |
| 6 | 网络搜索引用默认折叠 | ✅ 已完成 | 来源面板默认收起，展开后显示来源行；保留数量、可点击链接与错误状态 |
| 7 | 发送后收起键盘，AI 回复期间不自动聚焦回输入区 | ✅ 已完成 | 发送即收起键盘，回复期间与完成后保持收起；编辑消息仍会聚焦输入区 |


> **已知问题：论文聊天「读取全文」按钮部分场景点击后无反馈或停在加载态**
>
> **当前实现**：点击右上角书本按钮后异步执行——① 校验论文 pdfUrl；② 查本地缓存（`papers.pdf-extracts`，键 = paperId + PDF URL）；③ 未命中则 http 下载 PDF（60s 超时）→ `syncfusion_flutter_pdf` 逐页提取文本 → 按段落分块（约 1800 字符/块，记录起始页码）→ 缓存；④ `PaperPdfContextBuilder` 按 6000 字符预算裁剪并生成「【第 N 页】」标注的全文引用文本；⑤ 重建 `ChatContext` 把全文引用追加到 systemPrompt；⑥ `ChatConversationController.replaceContext` 替换会话上下文。之后请求的 systemPrompt 即携带全文分块。
>
> **问题原因**：`PaperAiChatScreen._toggleFullText` 仅 `catch (Exception)`；PDF 下载超时 / HTTP 非 2xx / `syncfusion` 解析抛 `Error`（如 ArgumentError）时异常外泄，`_fullTextLoading` 不复位且无错误提示，表现为点击无效果；成功时也无完成提示（仅图标变实心）。
>
> **修复方向（待排期）**：捕获所有异常并复位 loading；成功/失败均以 SnackBar 明确提示；失败时给出具体原因（下载失败 / 解析失败 / 无 PDF 链接）。
### 3.3 我的

| # | 任务 | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 收藏分组排序与批量移动 | ⬜ 待开始 | 数据层已保留顺序能力；删除分组目前会一并移除组内收藏关系 |
| 2 | 个人研究数据导出 / 导入 / 备份恢复 | ⬜ 待开始 | — |
| 3 | 统一数据占用统计、分类清理与迁移失败恢复 | 🚧 部分完成 | 论文缓存、聊天和业务数据的占用统计、分类清理、逐版本迁移、损坏隔离和中断写入恢复已实现；用户侧迁移失败的显式恢复流程仍待补齐 |

---

## 4. 设计规格参考

### 4.1 论文频道

#### 频道栏布局

```
推荐  关注  最新  人工智能  计算与语言  机器学习  ICML  ACL  ＋
```

- `推荐 / 关注 / 最新` 固定存在，不能删除
- 用户添加的主题和会议显示在后面
- 频道栏支持横向滚动；`＋` 固定在右侧，始终可见
- 每个频道独立保存浏览位置
- 点击论文后返回时仍在当前频道信息流
- 不再使用原来的领域筛选按钮

**会议频道**（保留官方缩写）：

```
AAAI   ACL    COLM   COLT   CoRL   CVPR
ECCV   EMNLP  ICCV   ICLR   ICML   IJCAI
INTERSPEECH  IWSLT  MICCAI  MLSYS  NAACL  NDSS
NeurIPS  OSDI  UAI  USENIX-Fast  USENIX-Sec
```

> **约束**：会议频道必须接入真实数据源后再进入正式版本，不能只提供空白 UI。

#### 时间筛选

原领域筛选按钮改为时间筛选，只作用于当前频道：

```
不限时间 / 最新发布日 / 最近 7 天 / 最近 30 天 / 选择日期 / 自定义时间范围
```

示例组合：`机器学习 + 最近 7 天`，`ACL + 2026 年`，`ICML + 2025 年`。
会议数据还可按年份和 Track 进一步筛选。

#### 论文元数据字段

**arXiv 主题论文**必须具备：arXiv ID、标题、作者、Abstract、Subjects、Publish Time、更新时间、原始论文地址、PDF 地址。

Subjects 示例：
```
Machine Learning (cs.LG)
Artificial Intelligence (cs.AI)
Computation and Language (cs.CL)
```

**会议论文**必须具备：标题、作者、Abstract、会议名称、会议年份、Track 或论文类型、原始来源、PDF 地址。

Subject 使用结构化格式：
```
ACL 2026 · Long Paper
ICML 2026 · Oral
CVPR 2026 · Highlight
NeurIPS 2025 · Poster
```

**引用数约束**：
- 引用数暂不以不可靠数据出现在产品界面
- arXiv 本身不提供可靠引用数；未增强的数据不能显示成"被引 0"
- 后续接入 OpenAlex 或 Semantic Scholar 后再恢复，并明确标记数据来源和更新时间

### 4.2 论文内容页面

#### 六页结构

```
Abstract  摘要  关键词  作者  AI 解读  相关论文
```

- 标签采用内容宽度和横向滚动，不做六等分矩形布局
- 每次切换论文默认回到 `Abstract`

#### Abstract

- Markdown 和公式渲染；文本可选择；内容超出后滚动
- 不进行两端对齐；保持紧凑行距

#### 摘要（中文翻译）

界面只显示内容状态，不向用户暴露内部实现细节：

```
生成中文摘要
正在生成...
生成失败，点击重试
```

不显示「DeepSeek 中文翻译」「思考模式已关闭」「使用某某模型生成」等文案。

#### 关键词

关键词必须来自论文内容，不能把 `cs.AI`、`cs.LG` 当作关键词。arXiv Subjects 和内容关键词必须分开存储。

生成规则：
1. 使用标题和 Abstract 提取关键词
2. AI 解读完成后，可用 PDF 全文重新生成更准确的关键词
3. 每篇论文保存约 5–12 个关键词，按重要程度排序
4. 结果写入本地缓存

示例：`低秩适配`、`参数高效微调`、`冻结预训练权重`、`低秩矩阵分解`、`大语言模型`

#### 作者

当前只展示完整作者列表，不展示不可靠的单位占位。第一作者、通讯作者、单位、作者主页和同作者论文等待 OpenAlex、论文 PDF 或其他可靠来源增强后再加入。

#### AI 解读

固定回答六个问题：
- Q1：这篇论文试图解决什么问题？
- Q2：有哪些相关研究？
- Q3：论文如何解决这个问题？
- Q4：论文做了哪些实验？
- Q5：有什么可以进一步探索的点？
- Q6：总结一下论文的主要内容。

初始状态不自动生成，用户点击后才执行：

```
生成 AI 解读
```

生成流程：
```
下载 PDF → 提取正文 → 清理页眉页脚和参考文献噪声
→ 按章节或长度分块 → 分块分析 → 汇总为六问结构化答案 → 保存到本地缓存
```

实现约束：
- DeepSeek 不直接上传 PDF，实现层先提取 PDF 文本，再分段发送给模型
- 缓存键至少包含：paperId、PDF 版本或文件哈希、提示词版本、模型版本、输出语言
- 论文更新或提示词结构改变后，判断旧缓存是否需要重新生成

#### AI 解读与 ChatPaper 的关系

AI 解读生成的六问答案作为论文 ChatPaper 会话的默认背景信息：

```
论文元数据 + Abstract + 关键词 + 六问 AI 解读
```

用户从论文页打开 AI 对话时：
- 自动关联当前论文；不需要重新粘贴论文信息
- AI 知道之前生成的六问答案；用户可继续追问实验、公式、局限性和相关工作
- 默认上下文不作为聊天消息重复显示

#### ChatPaper 上下文边界（主聊天 / 论文聊天 / 派生缓存）

三类上下文严格分离：

| 上下文 | 身份 | 内容来源 | 存储 |
| --- | --- | --- | --- |
| 主聊天 | `spark-main-ai-chat` 固定 id | 通用 systemPrompt，永不注入论文数据 | `chat_sessions.json` |
| 论文聊天 | contextId = 论文 id | 论文元数据 + Abstract + 有效缓存关键词；用户开启“读取全文”后追加按预算裁剪的 PDF 分块和页码引用 | `chat_sessions.json`；全文提取缓存为 `papers.pdf-extracts` |
| 论文派生缓存 | 论文级独立缓存 | 关键词 / 中文翻译 / 阅读状态；六问 AI 解读仍待实现 | `papers.*` 独立 schema |

边界规则：

- 会话仓储只持久化聊天消息（ChatMessage）；派生数据不进入消息，只作为 systemPrompt 背景注入
- 主聊天永不注入论文派生数据；论文聊天只注入当前论文的派生数据
- 关键词只注入「已缓存且版本有效」的数据；arXiv Subjects 与内容关键词分开，Subjects 不冒充关键词
- 派生缓存失效（论文更新 / prompt 版本变化）后新会话不再使用旧缓存；历史会话消息保持不变
- 六问 AI 解读结果作为论文聊天默认背景仍依赖论文模块第 9 条；当前已实现的是用户主动开启“读取全文”后注入 PDF 分块
#### 相关论文

暂保留页面和空状态，真实检索后续实现。
未来检索条件：论文标题语义、内容关键词、Abstract 向量、Subjects、作者关系、引用关系。
第一阶段用"标题 + 关键词"语义检索，之后加入全文向量和引用图谱。

### 4.3 本地缓存清单

**当前单机阶段**，以下内容按需生成并本地缓存：

| 缓存项 | 触发时机 |
| --- | --- |
| 中文摘要 | 用户在摘要页触发 |
| 内容关键词 | 用户在关键词页触发；缓存有效时同时进入 ChatPaper 上下文 |
| 六问 AI 解读 | 规划中；用户在 AI 解读页触发后生成 |
| PDF 提取结果 | 论文聊天用户点击“读取全文”后下载、提取并缓存 |
| 论文 AI 会话 | 用户主动创建或从论文页打开 |

所有能力按需生成，避免用户没有阅读意图时浪费 API 调用。

---

## 5. 远期方向

### 5.1 服务端与数据闭环（论文 + ChatPaper）

- 用户登录；点赞、收藏、评论、关注与搜索历史同步；本地未同步操作队列
- DeepSeek 后端代理：安全保存 Key、额度、限流、会话云持久化与成本控制
- 论文级共享派生数据：中文摘要、关键词、AI 解读、PDF 解析与语义向量的云端缓存（同一版本论文只需生成一次）
- arXiv 完整快照与增量同步；论文库接入

### 5.2 深度数据与推荐

- 结构化作者与机构、关键词、BibTeX、参考文献与引用关系
- 接入 OpenAlex 或 Semantic Scholar 引用数（明确标记来源与更新时间）
- 推荐系统利用阅读、停留、互动与关注信号，支持去重、排除已读和推荐原因

### 5.3 我的——账号同步

- 完善收藏分组排序、批量移动和删除保护
- 个人研究数据导出、导入与备份恢复
- 统一数据占用统计与分类清理
- 账号体系获得明确需求后，通过领域接口接入跨设备同步

### 5.4 社区与私信

前置依赖账号体系与服务端能力，就绪前保持 demo 代码不进入生产导航：

**社区**：补齐 application 层与仓储，用真实内容源和持久化替换演示数据；复用论文数据与互动模型，建立帖子、评论与论文讨论的关联。

**私信**：补齐 presentation 层、消息持久化与已读状态；明确私信、互动通知与系统消息的边界。

> **约束**：社区和私信进入生产导航时必须具备真实数据源与账号支撑，不以无行为入口出现。数据分层必须符合 `standards/code-structure.md`（补齐 application 层、仓储与 DTO 转换）。

---

## 6. 验收原则

### 论文

- 添加或移除频道后，频道栏和本地配置同步更新
- 每个频道的查询、分页、错误状态和浏览位置互不污染
- 时间筛选改变真实请求；离线时只使用匹配条件的缓存
- 未知单位和未知引用数不会显示为虚假数据；内容关键词不含仅用于分类的 arXiv 编号
- 每篇论文默认进入 Abstract，六页切换无闪烁和高度跳变
- AI 解读只在用户触发后下载和处理 PDF，失败可重试且不会破坏旧缓存
- AI 解读完成后，新建论文聊天能读取六问结果作为默认上下文

### ChatPaper

- 输入区固定，不被消息内容或键盘异常挤压
- 流式过程与最终内容布局稳定，不在结束时整体跳变
- 代码块可复制，公式与 Markdown 失败时仍有可读纯文本降级
- 每个会话的上下文、思考开关和联网状态行为明确且可恢复

### 我的

- 所有数据管理操作明确展示影响范围并可验证结果
- 收藏、历史和稍后阅读在应用重启后保持一致
- 密钥只显示掩码，不进入日志、文档、源码或普通本地数据文件
- 未实现的账号或社区能力不以无行为入口出现在生产导航中

---

## 7. 发布关系

开发任务默认不绑定版本。只有当一组已经实现并验证的能力被选入发布候选后，才在 `releases/<version>/` 建立版本说明（README.md），记录发布范围、交付物与已知问题。

当前尚未正式发布，没有发布归档。达到发布标准后由 `/release` 在 `releases/<version>/` 建立版本说明。发布归档记录历史事实，不反向成为日常功能开发的唯一入口。
