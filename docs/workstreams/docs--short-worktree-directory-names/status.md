# 任务台账

## 基本信息

- 任务：缩短新建任务 worktree 目录名
- 关联发布或里程碑：不关联发布（开发流程规范）
- 分支：`docs/short-worktree-directory-names`
- Worktree：`C:\Users\Fantasy\Desktop\Spark-worktrees\agent-5`
- 基线提交：`40b27922dae0cf84a381a8b9036b7b00ee329447`
- 负责人：Fantasy（编排者）；执行：Codex
- 状态：开发完成，待测试
- 最近更新：2026-08-12

## 目标

后续新任务使用 `agent-<n>` 短目录名创建 worktree，以降低 Windows 路径长度；Git 分支和任务台账继续保留可读任务语义。

## 非目标

- 不批量迁移或重命名当前仍在使用的旧 worktree。
- 不改变 Git 分支格式、台账目录格式、合并策略或验证门禁。
- 不删除现有任务目录或分支。

## 验收标准

- [x] `AGENTS.md` 和 Git 规范明确新 worktree 使用 `agent-<n>`，控制工作树不计入编号。
- [x] 编号从当前已注册任务 worktree 数量加一开始，目录冲突时继续递增。
- [x] 分支继续使用 `<type>/<slug>`，台账继续使用 `<branch-slug>`。
- [x] `/start` 指引不再使用分支 slug 或过时的 `PaperFlow-worktrees` 作为 worktree 目录。
- [x] 文档链接与 `git diff --check` 通过。

## 写入范围

### 独占路径

- `docs/workstreams/docs--short-worktree-directory-names/status.md`

### 共享路径

- `AGENTS.md`：只修改 worktree 目录命名规则。
- `docs/standards/version-control.md`：只修改 Worktree 隔离规范。
- `.claude/skills/start/SKILL.md`：只同步 `/start` 创建流程。

## 依赖关系

- 上游任务：无。
- 外部接口或数据源：无。

## 实施计划

1. 将 `agent-<n>` 的计数、冲突处理、分支解耦和旧目录兼容规则写入事实源。
2. 同步 `/start` 流程，静态检查文档和示例命令。
3. 提交文档检查点，等待编排者触发后续测试、审查和合并阶段。

## 当前进度

- 已完成：使用新规则创建 `agent-5` 和可读分支；更新规范与 `/start` 指引；完成文档静态验证。
- 正在进行：无。
- 下一步：提交开发检查点，等待编排者触发后续测试、审查和合并阶段。
- 阻塞项：无。

## 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-08-12 | 控制工作树不参与 `n` 计数 | `Spark` 是固定控制目录，不是任务 worktree | 当前 4 个任务 worktree 对应新目录 `agent-5` |
| 2026-08-12 | 目录冲突时从计数值继续递增 | 删除旧任务后编号可能不连续，裸计数可能碰撞 | 保持短名且不会覆盖现有目录 |
| 2026-08-12 | 不迁移现有旧命名 worktree | 活跃任务可能持有未提交状态或运行进程 | 新规则仅约束后续创建，避免干扰并行任务 |

## 验证记录

| 命令或人工检查 | 结果 | 日期 |
| --- | --- | --- |
| 新目录编号示例命令 | 当前 5 个已注册任务 worktree，下一个目录计算为 `agent-6` | 2026-08-12 |
| 变更文档相对链接检查 | 通过，未发现失效链接 | 2026-08-12 |
| 活跃规则旧路径扫描 | 通过；三份活跃规则中无 `PaperFlow-worktrees` 或分支 slug worktree 创建命令 | 2026-08-12 |
| `git diff --check` | 通过，无 whitespace 错误 | 2026-08-12 |

## 审查结论

- 审查日期：尚未进入 `/review`。
- 阻断项：尚未审查。
- 缺陷：尚未审查。
- 结论：待审查。

## 检查点与提交

| SHA | 提交信息 | 对应阶段 | 验证摘要 |
| --- | --- | --- | --- |

## 交付准备（合并前收集）

### 交付摘要

待合并前整理。

### 实际变更

- 领域与业务逻辑：无。
- 数据与基础设施：无。
- 界面与交互：无。
- 测试与工具：更新工作流示例命令。
- 文档：更新 Agent、Git 与 `/start` 规范。

### 兼容性与迁移

- 本地数据迁移：无。
- API 或领域契约变化：无。
- 旧版本兼容性：现有旧命名 worktree 保持可用。

### 已知风险与回滚

- 已知风险：不同任务并发创建时仍需在执行 `git worktree add` 前复核目录未占用。
- 回滚方式：revert 本任务文档提交；不涉及数据迁移。

### 文档更新建议

- 本任务不改变产品能力，`docs/development.md` 无需更新。

### 未完成与后续工作

- 无。

## 合并归档（合并后在 main 补齐）

- 最终状态：待合并
- 合入分支：`main`
- 最终集成提交：待合并后填写
- Pull Request：无
- 合并时间：待填写
- main 集成验证：待填写
- 开发计划更新：不适用（开发流程规范，不改变产品能力）
- 最终后续项：待合并后填写
