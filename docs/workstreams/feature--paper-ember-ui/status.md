# 纸感与火花 UI 改造

## 基本信息

- 任务：落实用户已确认的 Paper & Ember 视觉方案。
- 分支：`feature/paper-ember-ui`
- 基线：`7e71b36e7209da4e8d2385eba352b13d03e0fea0`（main）
- 状态：开发中
- 最近更新：2026-09-24
- 负责人：ChatGPT；需求与合并由仓库所有者确认。
- 工作区：通过 GitHub 连接在独立远程分支修改。当前执行环境无法解析 GitHub 域名，未成功创建本地 clone/worktree；不会声称已完成本地 worktree 隔离或运行验证。

## 目标与验收

- [ ] 暖纸白、石墨文字、火花橙与柔和深色主题。
- [ ] 发现页分层导航、阅读排版与主要操作层级。
- [ ] ChatPaper 上下文与输入区保持一致的主题语言。
- [ ] 我的研究页优先呈现研究资料，保留配置入口。
- [ ] 保留原有收藏、分组、稍后阅读、历史、评论、分享、频道、AI 与隐私功能。
- [ ] 补充主题、布局和交互回归测试；运行结果按实际记录。
- [ ] 用户 Windows/Android 实机验收。

## 非目标

不改后端、API、存储 schema、推荐算法或版本号；不增加账号、社区、通知与云同步；不嵌入字体文件或密钥；不生成虚构引用；不合并 main、不发布。

## 写入范围

- `lib/src/core/theme/`：主题、排版与结构 token。
- `lib/src/core/widgets/`：受影响的通用呈现组件。
- `lib/src/features/papers/presentation/`：发现、阅读与操作组件。
- `lib/src/features/chat/presentation/`：主题与上下文呈现。
- `lib/src/features/profile/presentation/`：研究资料布局。
- 相关 `test/` 文件与本任务台账。

## 实施计划

1. 调整语义色和排版，保留已有主题偏好。
2. 修改核心页面和组件，复用既有 controller/callback。
3. 补充测试并审阅差异，记录实际验证能力和待验收项。

## 决策

- 默认主题使用火花橙，已保存的其他强调色继续生效。
- AI 状态、论文元信息、页码和来源只取真实应用数据；设计稿中的示例不进入产品。
- 不自动发起新的付费摘要/全文请求，不改实验功能开关。
- 字体使用系统回退；不同平台字形需实机检查。

## 验证与阻塞

- 已核对 main 基线、README、开发计划、架构/版本/发布规范以及已归档苹果风主题台账。
- `git clone --depth=1`：网络解析失败。
- Flutter / Dart SDK：当前环境未安装；尚未运行 analyze、test 或平台构建。
- 本阶段未触发 /finish，不进行 main 合并或发布。

## 合并归档

未合并，待实现、验证、人工验收和明确合并授权。
