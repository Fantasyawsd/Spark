# 论文实体复制边界重构任务台账

> 状态：开发中
> 最近更新：2026-08-13 20:47

## 1. 任务信息

| 项目 | 内容 |
| --- | --- |
| 目标 | 为 `Paper` 建立单一、完整的复制入口，让增强合并只声明被覆盖字段，避免手工重建实体时静默丢失其余字段。 |
| 非目标 | 不改变 `Paper` 字段、持久化 schema、OpenAlex DTO、同步流程或产品展示；不处理其他报告问题；不合入 `main`，不进行人工桌面验收。 |
| 分支 | `refactor/paper-copy-boundary` |
| worktree | `C:\Users\Fantasy\Desktop\Spark-worktrees\agent-10` |
| 基线 | `011eaa121a4a432417510fab3c452712ee1e129e`（上一批 `refactor/paper-keyword-boundary` 最终审查提交） |
| 负责人 | Fantasy（编排）；Codex（执行） |
| 当前阶段 | `/develop` |

## 2. 问题与边界

`PaperEnhancementMerge.applyEnhancement()` 当前通过重新调用 `Paper(...)` 手工复制约 25 个字段，只覆盖单位、概念和引用数。`Paper` 增加字段时，增强合并不会得到编译器保护，遗漏复制会静默丢数据。本批把完整复制规则集中到 `Paper.copyWith()`，增强扩展只传入真正变化的字段。

保持不变：

- 空增强列表不覆盖原单位或内容关键词。
- 缺失引用数不覆盖原引用数，包括未知 `null`。
- `Paper` 构造器仍对集合创建不可变副本。
- 所有身份、内容、元数据、指标、链接和时间字段在增强后保持原值，除明确增强字段外。

## 3. 验收标准

1. `Paper` 提供覆盖全部当前构造字段的 `copyWith()`，复制结果继续保持集合不可变。
2. `applyEnhancement()` 不再手工枚举全部 `Paper` 字段，只覆盖非空单位、非空概念和非空引用数。
3. 测试使用字段完整的论文实体，证明增强前后所有非目标字段不变、目标字段按规则更新。
4. 空增强不会把已有增强字段清空；缺失引用数不会把已知值变成未知或把未知值伪造为 0。
5. 定向测试、`flutter analyze`、格式门禁和 `/test` 全量门禁全部通过。

## 4. 写入范围

### 独占路径

- `lib/src/features/papers/domain/paper.dart`
- `lib/src/features/papers/domain/paper_enhancement.dart`
- `test/paper_enhancement_test.dart`
- 本台账

### 共享路径

- 无；本批串行基于上一批最终审查提交创建。

## 5. 实施计划

1. 增加字段完整的增强合并测试，锁定目标字段覆盖和其余字段保留。
2. 在 `Paper` 中实现单一 `copyWith()`，保持构造器与不可变集合语义。
3. 将 `applyEnhancement()` 收窄为只声明三个增强字段。
4. 运行定向测试、analyze 和格式检查，形成原子实现提交。
5. 执行 `/test` 全量门禁和 `/review` 只读审查，记录最终串行基线。

## 6. 当前进度

- 已完成：复核报告与当前代码，确认相对时间缓存和三项服务端数据完整性问题已经修复，不重复处理。
- 已完成：确认 `applyEnhancement()` 仍手工复制全部字段，问题成立。
- 已完成：`Paper.copyWith()` 已覆盖全部当前构造字段，可空字段通过类型安全的显式 clear 标志清空。
- 已完成：`applyEnhancement()` 已收窄为只覆盖单位、内容概念和引用数；完整字段保留与集合不可变测试通过。
- 已完成：`/develop` 原子实现提交 `1fe06ac` 已创建。
- 已完成：`/test` 格式、静态分析和 542 项全量测试全部通过。
- 正在进行：回填 `/test` 证据并进入只读 `/review`。
- 下一步：对照规格与架构标准审查 `011eaa1..HEAD`。
- 阻塞项：无。

## 7. 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-08-13 | 将完整复制规则放入 `Paper.copyWith()`，增强扩展仅指定变化字段。 | 实体拥有自身复制语义；增强规则不应随着无关字段增长而同步扩张。 | 后续其他领域变换可复用同一入口，字段复制逻辑只维护一处。 |

## 8. 验证记录

| 时间 | 阶段 | 命令或检查 | 结果 |
| --- | --- | --- | --- |
| 2026-08-13 20:55 | `/develop` | `flutter test test\paper_enhancement_test.dart test\paper_sync_service_test.dart test\paper_sources_test.dart` | 通过，共 10 项；覆盖完整字段保留、三类增强覆盖、空增强、未知引用数、显式清空和集合不可变性。 |
| 2026-08-13 20:55 | `/develop` | `flutter analyze` | 通过，无问题。 |
| 2026-08-13 20:55 | `/develop` | `.\tool\verify_changed_dart_format.ps1` | 通过，共检查 113 个变更相关 Dart 文件。 |
| 2026-08-13 20:55 | `/develop` | `git diff --check` 与构造器/copyWith 字段对照 | 通过；27 个业务构造参数均有复制路径。 |
| 2026-08-13 20:47 | `/test` | `.\tool\verify_changed_dart_format.ps1` | 通过，共检查 113 个变更相关 Dart 文件。 |
| 2026-08-13 20:47 | `/test` | `flutter analyze` | 通过，无问题。 |
| 2026-08-13 20:47 | `/test` | `flutter test` | 通过，共 542 项测试；无失败、错误或超时。 |

## 9. 审查与交付

- 审查范围：待实现提交后填写。
- 阻断项：待审查。
- 缺陷：待审查。
- 审查结论：待审查。
- 兼容性：不修改 schema、远程接口或字段语义。
- 风险：复制入口漏列当前字段；通过字段完整测试和构造器对照审查约束。
- 回滚：按原子提交回滚，不触碰 `main`。

## 10. 检查点与提交

| SHA | 提交信息 | 对应阶段 | 验证摘要 |
| --- | --- | --- | --- |
| `1fe06ac` | `重构（论文）：集中实体复制规则` | `/develop` | 10 项定向测试、analyze、113 文件格式门禁通过；增强合并不再手工重建实体。 |
