# 收紧公共 API 导出边界任务台账

> 状态：待审查
> 最近更新：2026-08-13

## 目标

明确 `lib/spark.dart` 公共 barrel 的导出边界，禁止把数据适配器和未接线同步实现暴露为稳定公共 API，同时保留当前生产入口依赖的应用与领域类型。

## 非目标

- 不删除生产入口和现有测试实际使用的应用控制器导出。
- 不迁移现有调用方到内部路径。
- 不进行客户端人工验收，不合入 `main`。

## 分支与基线

- 分支：`refactor/spark-public-exports`
- Worktree：`C:\Users\Fantasy\Desktop\Spark-worktrees\agent-20`
- 基线：`58a66f1`

## 验收标准

- [x] 公共 barrel 不导出 `features/*/data` 实现。
- [x] 公共 barrel 不导出未接线的批量同步实现。
- [x] 现有公共 API 测试和完整门禁通过。
- [x] 只读审查结论记录为无阻断项。

## 实施计划

1. 复核当前公共导出与实际使用，确认数据层/同步实现是否已暴露。
2. 对确认越界的导出增加门禁测试或移除导出。
3. 执行完整验证并提交。

## 验证记录

| 命令或人工检查 | 结果 | 日期 |
| --- | --- | --- |
| `flutter pub get` | 通过 | 2026-08-13 |
| `dart format test/public_api_boundaries_test.dart` | 通过 | 2026-08-13 |
| `.\tool\verify_changed_dart_format.ps1` | 通过（132 个文件） | 2026-08-13 |
| `flutter analyze` | 通过 | 2026-08-13 |
| `flutter test` | 通过（554 项） | 2026-08-13 |
| `git diff --check` | 通过 | 2026-08-13 |

## 审查结论

只读审查结论：阻断项 0，缺陷 0，建议 0。复核确认当前公共 barrel 没有数据适配器或批量同步实现导出；新增静态门禁防止未来回归，未扩大 API 迁移范围。

## 检查点与提交

| SHA | 提交信息 | 对应阶段 | 验证摘要 |
| --- | --- | --- | --- |
