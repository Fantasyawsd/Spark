[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "spark-paper-platform"
version = "0.1.0"
description = "Offline-first paper data pipeline and versioned feed API for Spark"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
